version: "3.9"

services:

  app:
    build: .
    container_name: node-app
    ports:
      - "3000:3000"
    environment:
      REDIS_HOST: redis
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: redis-db
    ports:
      - "6379:6379"